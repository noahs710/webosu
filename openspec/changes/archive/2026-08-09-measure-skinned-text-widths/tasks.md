## 1. Measure

- [x] 1.1 Add `import.meta.env.DEV` gated log in `score.js` `setSpriteArrayText` for `width`/`orig.width`/`source.width`/`resolution`/`valid`/`effSpacing` per digit
- [x] 1.2 Run `npm run build` and capture table on default vs WhiteCat, dpr1 vs 2, `ScoreOverlap 0/2/4/6`

## 2. Deciding

- [x] 2.1 Recommend `orig.width` vs `getBounds()` and `charspacing`/`is2x` adjustment for `skinned-text-layout`

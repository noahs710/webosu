## 1. Measure

- [x] 1.1 Add DEV log for `getBounds()` before/after `_draw` and on `resize` for `cullable true` vs `false`
- [x] 1.2 Test edge sliders with `cullable true + cullArea` vs `false`

## 2. Lock

- [x] 2.1 Decide `cullable=false` vs `true+cullArea` for `slider-rendering`
